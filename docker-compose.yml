version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: workboard-postgres
    environment:
      POSTGRES_DB: workboard
      POSTGRES_USER: workboard
      POSTGRES_PASSWORD: workboard123
    ports:
      - "5432:5432"